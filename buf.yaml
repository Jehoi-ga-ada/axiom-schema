version: v2
modules:
  - path: proto
    name: buf.build/axiom-org/axiom-schema 
lint:
  use:
    - STANDARD
  except:
  - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE # Enforces wire-compatibility checks